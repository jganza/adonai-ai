services:
  - type: web
    name: adonai-ai
    runtime: node
    rootDir: .
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: PORT
        value: "3000"
    autoDeploy: true
